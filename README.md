# user-service
### Docker
in user-service run:\
`docker build -t user-service:latest .`

`docker compose up --build`


Zastarelo:\
and then:\
`docker run -p 8080:8080 user-service`

TODO:\
- friendship
- profile
- settings?
