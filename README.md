# user-service
### Docker
in user-service run:\
`docker build -t user-service .`

`docker compose up --build`


Zastarelo:\
and then:\
`docker run -p 8080:8080 user-service`

TODO:\
create channel
get user from channel
get channels form user
add user to channel
remove user from channel
